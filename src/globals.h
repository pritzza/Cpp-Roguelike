#ifndef GLOBALS_H
#define GLOBALS_H

// get this working

//Player player;
//Player* player_pointer = &player;
//
//int turn{};
//
//char map[consoleHeight][consoleWidth];      // Creates a 120x439 2D array (the size of the console by default) - 2
//char visibleMap[consoleHeight][consoleWidth];      // Creates a 120x439 2D array (the size of the console by default) - 2
//
//std::vector<Room> rooms;

#endif